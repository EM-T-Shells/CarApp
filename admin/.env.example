# Stabl admin panel — copy to .env.local and fill in. These are PUBLIC keys
# (the anon/publishable key) — never put the service_role key here. All
# privileged writes go through the admin-review-provider Edge Function.
#
# Use the SAME Supabase project as the mobile app (that is the "interlink"):
#   apbubklogxgqkokbctwz
VITE_SUPABASE_URL=https://apbubklogxgqkokbctwz.supabase.co
VITE_SUPABASE_ANON_KEY=
